# 中文译稿第三轮审校记录

修订日期：2026-09-05。范围为六章中文正文、README、目录、术语表及相应的知乎导入版本。英文源版本保持为 `fc66957a6458842beeabe9d85065ff334ccbd333`（2026-07-25），此次没有更新英文原作、改动原图或公开发布。

## 审校原则

- 语言：拆开过长句子，减少逐词对应和重复概括，补足指代与段落衔接；保留作者的论述顺序和工程视角。
- 术语：区分处理器核与计算内核、体系结构寄存器与物理寄存器，以及软件可见参数与实现参数。
- 技术：对照英文核查语义，针对发现的疑点查阅 RVV 规范；必要的纠正或补充在正文中标成译注或图中代码说明。
- 性能：说明算例的单位、计数边界和理想化前提，不把指令数减少直接写成实际加速比。
- 来源：保留作者、出版方、源 commit 与原有引用；不把无法核实的外链当作已验证文献。

这不是逐字逐句的规范审计，也不是作者确认的勘误版。译稿仍可能存在未发现的问题。

## 重要纠正

| 位置 | 本轮处理 |
|---|---|
| 第 1 章 1.14、1.15；第 2 章 2.9 | 宽向量结果不一定放在 ROB 表项中；区分 ROB、物理寄存器和结果缓冲区。 |
| 第 1 章 1.14.5；第 3 章 3.4.1.1 | CPU 统一管理配置是作者的实现分工，不意味着 VPU 没有执行状态，也不排除后续访存异常。 |
| 第 1 章 1.15、1.16 | 改正 LMUL=16 示例、SEW>64 的保留编码、FLEN 与 XLEN 的关系，以及 DLEN 与 EMUL 的区别。 |
| 第 1、2、3、5 章 | 区分 chaining 与具体旁路结构；RVV 不强制实现 chaining，定宽 SIMD 也可能拆分微操作。 |
| 第 2 章 2.2、2.3 | 配置裁剪须满足声明扩展的要求；无整数条件码不等于不存在 `fflags`、`vxsat` 等粘滞标志。 |
| 第 2 章 2.6 | “32 个元素、8 个通道”不能独立推出四倍加速；输入拍数不等于总执行延迟。 |
| 第 3 章 3.2、3.8.1 | 补全 AVL 与 VL 的三段规则，修正 `rd=rs1=x0` 保持 VL 形式的 VLMAX 约束。 |
| 第 3 章 3.3.2 | 区分 VRF bank 冲突与缓存/内存 bank 冲突，避免把单寄存器微操作粒度写成 ISA 要求。 |
| 第 3 章 3.5；术语表 | 普通 agnostic 数据元素保留旧值或写全 1，不能概括为任意位模式；掩码结果尾部另有规则。 |
| 第 3 章 3.8.2；第 4 章 4.1.3 | fault-only-first 的“首”是元素索引 0；后续同步异常将 VL 缩短到故障索引，不是成功访问次数。 |
| 第 3 章 3.9.4 | “先提交、后执行”是作者的阶段划分；离开 CPU ROB 不能自动解决精确陷入问题。 |
| 第 3 章 3.10；第 4 章 4.2.1 | 纯扩展的目的 EEW=SEW、EMUL=LMUL，源宽度及分组除以扩展倍数；补充源目的重叠约束。 |
| 第 4 章 4.1.1、4.1.2、4.1.5 | 索引访存指令编码的 EEW 描述索引，数据仍采用 SEW/LMUL。 |
| 第 4 章 4.1.4 | 零步长访问次数取决于寄存器字段编码；无序的固定步长存储不保证“最后一个元素胜出”。 |
| 第 4 章 4.1.6 | `NFIELDS=nf+1`；补充每字段 EMUL、对齐、总量和段内顺序约束，原图的简写不改动。 |
| 第 4 章 4.2.3 | 进位指令序列的软件数据保护与单条指令的非法重叠限制是两回事。 |
| 第 4 章 4.2.8 | 归约包含 `vs1[0]` 初始值，结果在 `vd[0]`；树深度不直接等于整条指令延迟。 |
| 第 4 章 4.3、4.4 | 明确 `viota` 为不含当前位置的前缀计数；补充 slide、gather 边界及整寄存器移动的 SEW/evl 语义。 |
| 第 5 章 | 区分单矩阵内向量化与跨矩阵实例并行；VLEN 增大不保证吞吐同比增加，连续加载替换离散访存须保持语义。 |
| 第 6 章 | 区分软件 tile 与物理 PE 阵列；向量也能长期保存部分和，循环控制主要由标量核完成。 |
| 第 6 章图 6-3 | 4,096 是 K 分块后的更新次数，输出块只有 256 个；修正 VL/VLEN 混用、分支和字节步进的图示问题。 |
| 第 6 章第 5 节 | 矩阵规范状态限定于英文源版本；`mma.mm` 是示例助记符，不是已确定的标准承诺。 |

## 规范依据

主要核对文本是 RISC-V 官方归档仓库中的 [RVV 1.0 文本](https://github.com/riscvarchive/riscv-v-spec/blob/v1.0/v-spec.adoc)。该标签文件保留了“冻结供公开审查”的历史说明；本记录引用它用于定位条款，不把它当成最新 ISA 手册快照。重点条款如下：

- 第 3.4 节：SEW、LMUL、agnostic 策略和 `vill`。
- 第 5.2、5.4 节：操作数 EEW/EMUL、寄存器重叠、活动及尾部元素。
- 第 6.2、6.3 节：AVL 编码及 VL 选择约束。
- 第 7.2、7.3、7.5、7.7、7.8 节：索引宽度、步长、fault-only-first 和分段访存。
- 第 11.3、11.4 节：整数扩展与进位/借位指令。
- 第 14、15、16 章：归约、掩码及置换；尤其是 `viota`、slide、gather、整寄存器移动。
- 第 17 章：精确陷入与恢复。

另核对了官方 ISA 手册的 [V 扩展章节](https://github.com/riscv/riscv-isa-manual/blob/main/src/unpriv/v-st-ext.adoc)，确认完整 V 对 FP32/FP64、F/D、Zve64d、Zvl128b 及精确陷入的要求。该链接指向持续更新的主分支，查阅日期为 2026-09-05。

第 2、3 章若干原有图注的规范节号与归档文本不符，已改用准确的节标题定位，保留 RISC-V International 和 CC BY 4.0 的原有署名信息。

## 保留的限制

- 第 5 章两条 VentureBeat 引用在复核时均返回 HTTP 429 访问验证；标题、年份和正文尚未独立核实，书目信息按原作保留。
- 原图保持不变。重点代码图的已发现问题已说明，但未对 62 张图中的每个编码位、箭头和数字进行完整独立审计。
- 第 6 章前三张图片在英文仓库中使用 `.png` 扩展名，文件实际为 JPEG；本地已有 PNG 副本，其字节和少量解码像素与仓库文件不同。这些差异在本轮修订前已存在，本次全部保留。知乎版仍按既有要求使用原仓库 URL，未重新验证知乎平台的实际导入行为。
- 没有执行硬件性能仿真，也没有把图中简化 intrinsic 和汇编示意当作可编译程序验证；性能讨论属于条件化分析。
- 第三轮语言审校结束后，译者补充了原作者的授权邮件记录，现已据此更新授权状态并加入隐去私人信息的摘要，见[翻译说明](TRANSLATION-NOTICE.md)。此次只更新文档，没有执行发布。

## 文件检查

正文是唯一维护源。知乎副本仅转换图片 URL，并去掉开头的本地翻译说明链接；不单独修改正文内容。检查覆盖 Markdown 解析、章节标题、表格列数、代码围栏、本地链接、图片存在性及图序，并比较英文原作和原图是否保持不变。

第三轮语言审校结束时的检查结果：

- 18 份 Markdown 均可由 Marked 解析并转为 HTML；标题层级、表格列数和代码围栏检查通过。
- 92 处本地链接及图片引用的目标存在，六章的图序与英文正文一致。
- 六章共引用 62 张图片，与本轮开始前的本地图片逐文件哈希一致。
- 六份知乎副本与主稿在上述路径转换后逐字一致，README 与目录中的章节标题一致。
- 英文 README、LICENSE 和六章正文与 Git HEAD 一致；未修改英文源文件。
- 复算了文中关键容量、EMUL、分块计数及 `viota` 示例。这些检查不替代完整指令模拟或硬件验证。
